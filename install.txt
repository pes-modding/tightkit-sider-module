1. copy tightkit.lua into your sider's "modules" folder
2. add this line to sider.ini:

lua.module = "tightkit.lua"

IMPORTANT: if you use Kitserver (kserv.lua), then make sure
to place tightkit.lua below kserv.lua in the list of modules
